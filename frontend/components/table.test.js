import React from 'react';
import { shallow } from 'enzyme';
import Table from './table';

describe('Table Component', () => {
  it('should render the table in the main com', () => {
    console.log('yeah');
  });
});
